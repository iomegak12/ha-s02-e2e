namespace Nexus.Identity.Api.Tests;

/// <summary>
/// Phase 0 placeholder so the test project compiles and runs in CI.
/// Real test suites land alongside each feature phase.
/// </summary>
public class SmokeTests
{
    [Fact]
    public void Sanity_TrueIsTrue()
    {
        Assert.True(true);
    }
}
